const numbers = [4, 9, 16, 25];
const newArr = numbers.map(Math.sqrt)
console.log(newArr)

const numbers1 = [4, 9, 16, 25];
numbers1.map(function(test){
    console.log(test) // map ini untuk mengetahui satuan bilangan array
})

const numbers2 = [4, 9, 16, 25];
numbers1.map(function(input){
     console.log(input) // map ini untuk mengetahui satuan bilangan array
})
